"""shellmate — a companion in your shell."""

# Must match the latest published git tag. If this is lower than the newest
# release, the update check tells every user — including one on a fresh install
# of the newest code — that an update is available, and the nag never clears.
__version__ = "1.0.0"
